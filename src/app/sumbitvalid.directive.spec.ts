import { SumbitvalidDirective } from './sumbitvalid.directive';

describe('SumbitvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new SumbitvalidDirective();
    expect(directive).toBeTruthy();
  });
});
